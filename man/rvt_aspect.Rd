% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/terrain.R
\name{rvt_aspect}
\alias{rvt_aspect}
\title{Aspect}
\usage{
rvt_aspect(
  dem,
  out_path = fs::file_temp(ext = "tif"),
  units = c("degree", "radian"),
  tile_size = NULL,
  threads = rvt_threads(),
  overwrite = FALSE,
  progress = FALSE
)
}
\arguments{
\item{dem}{path to a DEM, or a vector of paths forming a mosaic (see
\code{\link[=rvt_mosaic]{rvt_mosaic()}}); mosaics are read across file boundaries, so tiles do not
produce seams. A terra \code{SpatRaster} works too: one read from a file is
used as it lies, while one terra computed in memory is written to a
temporary GeoTIFF first.}

\item{out_path}{output GeoTIFF path (default: a temp file)}

\item{units}{\code{"degree"} (default) or \code{"radian"}}

\item{tile_size}{tile edge in pixels; NULL picks a size targeting roughly
256 MB per working matrix}

\item{threads}{C++ threads (see \code{\link[=rvt_threads]{rvt_threads()}})}

\item{overwrite}{recompute even if \code{out_path} exists}

\item{progress}{report per-tile progress}
}
\value{
\code{out_path}, invisibly
}
\description{
The compass direction each cell faces - the way water would run off it.
0 is north, 90 east, 180 south, 270 west.
}
\details{
Aspect on its own is rarely a useful picture: it wraps around at north, so
a smooth north-facing slope shows a hard seam between 359 and 0, and flat
ground points in an essentially arbitrary direction. It earns its place as
an input to other things - solar or ecological modelling, or splitting a
slope map into the sides of a feature.
}
\section{How it works}{

Taken from the same finite-difference derivatives as \code{\link[=rvt_slope]{rvt_slope()}}, as the
azimuth of steepest descent, then wrapped into 0-360. Cells on perfectly
flat ground have no meaningful aspect; the underlying formula carries a
small numerical guard so they come out as a fixed direction rather than
undefined, which is worth knowing before you read anything into large
uniform patches.
}

\section{Tuning}{

Nothing to tune. If you want a \emph{readable} directional image rather than raw
azimuths, \code{\link[=rvt_hillshade]{rvt_hillshade()}} or \code{\link[=rvt_asvf]{rvt_asvf()}} are what you actually want -
both fold aspect into a shaded result without the wrap-around discontinuity.

Note this will not match \code{gdalraster::dem_proc(mode = "aspect")} exactly.
That uses Horn's eight-neighbour estimate; this uses the four-neighbour
central difference, so that slope and aspect here stay consistent with each
other and with \code{\link[=rvt_vat]{rvt_vat()}}. On the bundled tile the two agree to about 1.4
degrees on slopes above 20 degrees but diverge to 33 degrees on ground
below 1 degree - which is not a disagreement about the terrain so much as
aspect being ill-defined when there is barely a gradient to point along.
}

\examples{
dem <- system.file("extdata", "dtm1.tif", package = "rvtr")
dem |> rvt_aspect()
}
\seealso{
\code{\link[=rvt_slope]{rvt_slope()}}, which shares the same derivative pass.
}
