% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/curvature.R
\name{rvt_curvature}
\alias{rvt_curvature}
\title{Curvature}
\usage{
rvt_curvature(
  dem,
  out_path = fs::file_temp(ext = "tif"),
  type = c("profile", "plan", "tangential", "mean", "minimal", "maximal", "gaussian",
    "total", "unsphericity", "casorati", "shape_index", "difference", "twisting",
    "rotor"),
  radius = NULL,
  tile_size = NULL,
  threads = rvt_threads(),
  overwrite = FALSE,
  progress = FALSE
)
}
\arguments{
\item{dem}{path to a DEM, or a vector of paths forming a mosaic (see
\code{\link[=rvt_mosaic]{rvt_mosaic()}}); mosaics are read across file boundaries, so tiles do not
produce seams. A terra \code{SpatRaster} works too: one read from a file is
used as it lies, while one terra computed in memory is written to a
temporary GeoTIFF first.}

\item{out_path}{output GeoTIFF path (default: a temp file)}

\item{type}{one of \code{"profile"} (default), \code{"plan"}, \code{"tangential"},
\code{"mean"}, \code{"minimal"}, \code{"maximal"}, \code{"gaussian"}, \code{"total"},
\code{"unsphericity"}, \code{"casorati"}, \code{"shape_index"}, \code{"difference"},
\code{"twisting"}, \code{"rotor"}}

\item{radius}{half-width of the window a quadratic surface is fitted over,
in \strong{map units}; omit it to measure across single pixels instead}

\item{tile_size}{tile edge in pixels; NULL picks a size targeting roughly
256 MB per working matrix}

\item{threads}{C++ threads (see \code{\link[=rvt_threads]{rvt_threads()}})}

\item{overwrite}{recompute even if \code{out_path} exists}

\item{progress}{report per-tile progress}
}
\value{
\code{out_path}, invisibly
}
\description{
How sharply the ground bends, and in which sense: \strong{positive is convex}
(the crest of a bank, the lip of a terrace), \strong{negative concave} (the
floor of a ditch, the foot of a slope), zero where the surface is locally
planar - whether that plane is flat or steeply tilted.
}
\details{
Curvature is the natural detector for \emph{breaks of slope}, which is often
what actually defines an earthwork: a bank is a convex line beside a
concave one. It is also the sharpest of the metrics here, in both senses -
it responds to the finest detail in the DEM, and it responds just as
eagerly to noise. Expect to need a smoothed DEM, or to prefer
\code{\link[=rvt_log]{rvt_log()}} which folds the smoothing in.

Units are per map unit (1/m on a metric DEM), and values are small: a
subtle bank might reach 0.05. There is no natural range, so displaying it
means picking a symmetric stretch around zero - something like -0.1 to 0.1
\itemize{
\item and a diverging palette so convex and concave read as opposite.
\code{shape_index} is the exception: it is dimensionless and already bounded to
-1 to 1, so it displays as it comes.
}
}
\section{How it works}{

A quadratic surface (Zevenbergen and Thorne 1987) is fitted to the eight
neighbours of each cell, and the named curvatures are combinations of its
second derivatives:

\describe{
\item{\code{profile}}{\strong{Does water speed up or slow down here?} Positive where
a slope steepens towards the brow of a bank, negative where it eases off
at the foot. Measured straight down the slope. This is the one that
picks out breaks of slope, and usually what people mean by "curvature"
unqualified.}
\item{\code{plan}}{\strong{Does water gather together or spread apart here?}
Negative in hollows and gullies where flow converges, positive on spurs
and noses where it fans out. Measured across the slope, along the
contour.}
\item{\code{tangential}}{The same gathering-or-spreading question as \code{plan},
but better behaved on gentle ground, where contours wander and plan
curvature turns erratic. Prefer it on anything flat.}
\item{\code{mean}}{\strong{Does the ground bulge outwards or dish inwards here?}
Averaged over every direction at once. The general-purpose convexity
measure, and the only one of these that stays meaningful on level
ground.}
\item{\code{minimal},\code{maximal}}{\strong{Which way does the ground bend most, and
which way least?} A ridge bends hard across its crest and barely at all
along it, so \code{maximal} is large and positive while \code{minimal} sits near
zero; a valley is the mirror of that. A dome has both positive, a pit
both negative.}
\item{\code{gaussian}}{\strong{Is this a dome or bowl, or a saddle?} Positive where
the ground bends the same way in every direction, negative where it
bends up one way and down the other - passes between two summits, or the
junction of two valleys.}
\item{\code{total}}{\strong{How much is happening here at all?} How strongly the
ground bends in any direction, ignoring whether it bends up or down. A
magnitude, so never negative - useful for finding where something is
going on when you do not care what.}
\item{\code{unsphericity}}{\strong{Is the bending even or lopsided?} Near zero where
the ground curves the same way in every direction - a dome, a bowl, a
plane - and large on ridges, valleys and saddles that bend mostly one
way. Never negative.}
\item{\code{casorati}}{\strong{How sharp is the bending, whatever shape it makes?}
Zero only where the ground is genuinely flat. Like \code{total} in spirit but
a properly normalised curvature, so it stays comparable between gentle
and steep ground.}
\item{\code{shape_index}}{\strong{What shape is the ground, regardless of how hard it
bends?} A pit reads -1, a valley -0.5, a saddle 0, a ridge +0.5 and a
peak +1. It separates form from intensity, so a faint ridge and a sharp
one read alike, and being bounded it needs no stretch to display.}
\item{\code{difference}}{\strong{Is this place more about water speeding up, or more
about water gathering?} Mean curvature averages those two and can read
zero where a strong acceleration cancels a strong convergence; this is
the distinction that goes missing there.}
\item{\code{twisting}}{\strong{Is the hillside twisting?} How fast the steepness
changes as you walk \emph{sideways} across a slope rather than up or down it.
Zero on any slope of even steepness, however steep. Positive where the
ground is steeper to your right as you look downhill. It is exactly the
rate of change of slope angle along the contour, in radians per map
unit, so the number means something directly.}
\item{\code{rotor}}{\strong{Does the downhill path bend as it descends?} Positive
where a ball rolling down would veer right, negative left, zero where it
runs straight. Picks out corkscrewing spurs and gullies that turn as
they fall - a thing none of the others above can see, because they all
ask about bending \emph{across} or \emph{along} the slope, never about the slope
direction itself changing. It is the projected form of \code{twisting} and
shares \code{plan}'s weakness for the same reason: on near-level ground the
downhill direction is barely defined, so values explode (hundreds,
against \code{twisting}'s fraction of one). Use \code{twisting} unless you
specifically want the plan-view bend.}
}

\code{unsphericity}, \code{casorati}, \code{shape_index} and \code{difference} come from Shary's
system, of which \code{mean}, \code{unsphericity} and \code{difference} are the three
independent components - every other curvature here is a combination of
those. \code{twisting} completes the basic trio of \code{profile}, \code{plan} and
twisting, and \code{rotor} is its projected form.

Except for \code{total}, these are the proper differential-geometry quantities,
carrying the \code{(1 + slope^2)} denominators that turn second derivatives into
actual curvature. Dropping those - as some implementations do - agrees only
where the ground is level and overstates curvature increasingly with slope,
by more than a factor of two by 17 degrees. \code{total} is the exception on
purpose: it follows Evans' un-normalised definition, because that is what
the name means in the geomorphometry literature and what other software
returns for it.

Checked against closed-form values on a tilted paraboloid and against
WhiteboxTools: \code{mean}, \code{gaussian}, \code{minimal}, \code{maximal} and \code{profile} agree
with both to four significant figures, and \code{total} matches WhiteboxTools.

Give a \code{radius} and the scale changes: a quadratic surface is fitted by
least squares over the whole window (Evans 1980; Wood 1996) and the same
curvatures are read off its coefficients. This is the standard way to pick a
scale, and the only one available - the Zevenbergen and Thorne fit passes
exactly through nine points and has no wider form. The two are not identical
at the smallest radius: the fit averages the bending over every row of the
window, where the default takes the middle row alone. Leaving \code{radius} out
therefore keeps the values other software returns, and setting it opts into
the multi-scale form.

Profile and plan curvature need a slope direction to be defined at all; on
genuinely flat cells they are reported as 0 rather than as a division by
nearly nothing. NoData inside the window contributes the centre cell's own
elevation, so a hole never eats a ring of cells around itself - but at a
large \code{radius} that biases curvature towards zero near big holes, so fill
them first with \code{\link[=rvt_fill]{rvt_fill()}}.
}

\section{Tuning}{

\itemize{
\item \code{type} is the only real choice, and \code{"profile"} is the usual starting
point for finding earthworks.
\item \code{radius} sets the scale, in \strong{map units}. Omitted, curvature is measured
across single pixels, which on a noisy DEM shows. A few metres steadies it
and picks out banks and ditch lips; tens of metres describes the shape of
the hillslope instead. Cost grows with the square of the radius, so keep
it to what the features need.
\item \code{\link[=rvt_log]{rvt_log()}} is the alternative when you want an edge detector rather than
a curvature: its \code{sigma} sets a scale too, but by smoothing first.
}
}

\examples{
dem <- system.file("extdata", "dtm1.tif", package = "rvtr")
rvt_curvature(dem)
rvt_curvature(dem, type = "plan")
rvt_curvature(dem, radius = 5)
}
\references{
Zevenbergen, L. W. and Thorne, C. R. (1987) Quantitative analysis of land
surface topography. \emph{Earth Surface Processes and Landforms} 12, 47-56.

Evans, I. S. (1980) An integrated system of terrain analysis and slope
mapping. \emph{Zeitschrift für Geomorphologie} Supplementband 36, 274-295.

Wood, J. (1996) \emph{The Geomorphological Characterisation of Digital Elevation
Models}. PhD thesis, University of Leicester.

Shary, P. A. (1995) Land surface in gravity points classification by a
complete system of curvatures. \emph{Mathematical Geology} 27, 373-390.
\doi{10.1007/BF02084608}

Koenderink, J. J. and van Doorn, A. J. (1992) Surface shape and curvature
scales. \emph{Image and Vision Computing} 10, 557-564.
\doi{10.1016/0262-8856(92)90076-F}

Minár, J., Evans, I. S. and Jenčo, M. (2020) A comprehensive system of
definitions of land surface (topographic) curvatures. \emph{Earth-Science
Reviews} 211, 103414. \doi{10.1016/j.earscirev.2020.103414}
}
\seealso{
\code{\link[=rvt_log]{rvt_log()}} for an edge detector with a scale parameter.
}
