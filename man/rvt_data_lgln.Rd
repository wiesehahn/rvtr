% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_lgln.R
\name{rvt_data_lgln}
\alias{rvt_data_lgln}
\title{Lower Saxony elevation and orthophotos, for any place}
\usage{
rvt_data_lgln(
  x,
  product = c("dtm", "dsm", "rgb"),
  year = NULL,
  res = NULL,
  download = FALSE,
  max_mb = 500,
  refresh = FALSE,
  threads = rvt_threads()
)
}
\arguments{
\item{x}{location: a WGS84 \code{c(lon, lat)} point or \code{c(xmin, ymin, xmax, ymax)} extent, an sf point, geometry or bbox, or the path to an
EPSG:25832 raster}

\item{product}{\code{"dtm"} (default), \code{"dsm"} or \code{"rgb"}}

\item{year}{flight year, or \code{NULL} (default) for the most recent that covers
the whole area}

\item{res}{output resolution in metres, or \code{NULL} (default) for native -
or, when \code{x} is a raster, for that raster's own resolution}

\item{download}{\code{FALSE} (default) returns a virtual raster reading the
remote files on demand; \code{TRUE} stores a local copy in the user cache}

\item{max_mb}{stop before reading if the estimated download exceeds this
many megabytes (default 500)}

\item{refresh}{with \code{download = TRUE}, fetch again even if cached}

\item{threads}{C++ threads (see \code{\link[=rvt_threads]{rvt_threads()}})}
}
\value{
path to the raster (a VRT, or a Cloud-Optimized GeoTIFF with
\code{download = TRUE}), so it pipes into any function
}
\description{
Terrain model, surface model or orthophoto for any location in Lower Saxony,
from the state survey authority (LGLN), in any flight year it holds. Give a
point to get the 1 km tile it lies in, an extent to get exactly that area, or
a raster to get data on its grid.
}
\details{
Nothing is downloaded by default. The result is a small virtual raster that
points at LGLN's published Cloud-Optimized GeoTIFFs, and data moves only when
something reads it - and only the parts that read needs. Pass it to any
function in this package. \code{download = TRUE} stores a local copy instead, for
offline work or repeated sessions.\tabular{llll}{
   product \tab what \tab native grid \tab years \cr
   \code{"dtm"} \tab DGM1 terrain model, the bare ground \tab 1 m, 1 km tiles \tab usually one \cr
   \code{"dsm"} \tab DOM1 surface model, ground plus trees and buildings \tab 1 m, 1 km tiles \tab usually one \cr
   \code{"rgb"} \tab DOP20 orthophoto \tab 0.2 m, 2 km tiles \tab several, e.g. 2013-2025 \cr
}


Everything comes in ETRS89 / UTM zone 32N (EPSG:25832), the data's own
coordinate system; locations are transformed to find the data, but the data
is never reprojected. \code{rvt_data_lgln_years()} lists which years exist for a
place without fetching any data.
}
\section{Location}{

\itemize{
\item \strong{A point} returns the 1 x 1 km tile containing it. Plain numbers
\code{c(lon, lat)} are WGS84; an sf point uses its own coordinate system, so
EPSG:25832 coordinates work as an sf point.
\item \strong{An extent} returns exactly that area, snapped outward to whole cells:
an \code{sf::st_bbox()}, any sf geometry, or plain WGS84
\code{c(xmin, ymin, xmax, ymax)}.
\item \strong{A raster path} returns data on exactly that raster's grid, which must
be EPSG:25832.
}
}

\section{Resolution and download size}{

\code{res = NULL} keeps native resolution. A number resamples, cubic, onto a grid
of whole multiples of \code{res}, so products fetched for the same place at the
same \code{res} line up exactly and can go straight into \code{\link[=rvt_blend]{rvt_blend()}}.

Coarser requests read from the files' overviews, which cuts orthophoto
downloads roughly tenfold at 1 m. \strong{The terrain and surface files have no
overview coarser than 2 m}, though, so even a 100 m request still reads
about 1 MB per km² - all of Lower Saxony would be around 50 GB. Before
anything is read, the download is estimated and the call stops above
\code{max_mb}. For large areas at coarse resolution, use a coarse elevation
source instead.
}

\section{Years}{

\code{year = NULL} uses the most recent year that covers the whole area. Flights
in one year can be spread over several dates between neighbouring tiles, so
years are chosen, not dates; within a year each tile uses its latest flight.
}

\section{Licence}{

LGLN open geodata, licensed CC BY 4.0 under section 7 of LGLN's terms of
use. Credit them as \strong{© GeoBasis-DE/LGLN} followed by the year you
downloaded the data, and add \strong{", Daten geändert"} (data modified) for
anything derived from them.
}

\examples{
\donttest{
pt <- c(9.9464, 51.6317)                      # Burg Hardenberg
rvt_data_lgln_years(pt, "rgb")
rvt_data_lgln(pt, "dtm") |> rvt_hillshade() |> rvt_plot()
rvt_data_lgln(pt, "rgb", year = 2013, res = 1) |>
  rvt_plot(range = c(0, 255))
}
}
\seealso{
\code{\link[=rvt_data_lgln_years]{rvt_data_lgln_years()}}
}
