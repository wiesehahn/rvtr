% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_mapterhorn.R
\name{rvt_data_mapterhorn}
\alias{rvt_data_mapterhorn}
\title{Terrain for anywhere in the world, from Mapterhorn}
\usage{
rvt_data_mapterhorn(
  x,
  res = NULL,
  size = 1000,
  crs = NULL,
  download = FALSE,
  max_mb = 500,
  refresh = FALSE,
  threads = rvt_threads()
)
}
\arguments{
\item{x}{location: a WGS84 \code{c(lon, lat)} point or \code{c(xmin, ymin, xmax, ymax)} extent, an sf point, geometry or bbox, or the path to a raster in a
projected coordinate system}

\item{res}{output resolution in metres, or \code{NULL} (default) for the finest
source available}

\item{size}{side of the square returned for a point, in metres (default 1000)}

\item{crs}{output coordinate system: an EPSG code, \code{"EPSG:..."} string or
WKT, in metres. \code{NULL} (default) uses the local UTM zone.}

\item{download}{\code{FALSE} (default) returns a virtual raster reading the
tiles on demand; \code{TRUE} stores a local copy in the user cache}

\item{max_mb}{stop before reading if the estimated download exceeds this
many megabytes (default 500)}

\item{refresh}{with \code{download = TRUE}, fetch again even if cached}

\item{threads}{C++ threads (see \code{\link[=rvt_threads]{rvt_threads()}})}
}
\value{
path to the raster (a VRT, or a Cloud-Optimized GeoTIFF with
\code{download = TRUE}), so it pipes into any function
}
\description{
A digital terrain model for any place on Earth, from
\href{https://mapterhorn.com}{Mapterhorn}, which combines open elevation data into
one global dataset: 30 m Copernicus GLO-30 everywhere, and national LiDAR
terrain models where they have been published - 1 m across Germany, Austria
and Norway, 0.5 m in Switzerland, 0.25 m in parts of Baden-Württemberg, and
many more. Give a point to get a square around it, or an extent to get
exactly that area.
}
\details{
Nothing is downloaded by default. The result is a small virtual raster that
reads Mapterhorn's tiles only when something reads it, and only the tiles
that read needs. Pass it to any function in this package.
\code{download = TRUE} stores a local copy instead, for offline work or repeated
sessions.
}
\section{Resolution}{

\code{res = NULL} uses the finest source covering the area; see what that is with
\code{\link[=rvt_data_mapterhorn_sources]{rvt_data_mapterhorn_sources()}}. Asking for a finer \code{res} than that is an
error, because it could only interpolate. Where the area straddles sources
of different resolution - the edge of a national LiDAR survey, say - the
coarser source fills in wherever the finer one has no data, so the result
has no holes.

Heights are stored to 1/256 m (about 4 mm), which is below the accuracy of
any source.
}

\section{Coordinate system}{

Mapterhorn's tiles are in Web Mercator, whose map units are stretched by
1/cos(latitude): 1.6 times at 51 degrees north. Since every distance in this
package is in map units, the data is always reprojected to a coordinate
system in metres - by default the UTM zone of the area's centre (standard
zones, e.g. EPSG:32632), or whatever \code{crs} you pass. Pass the national
system to line data up with other sources, e.g. \code{crs = 25832} for Germany.
}

\section{Location}{

\itemize{
\item \strong{A point} returns a \code{size} x \code{size} m square centred on it. Plain
numbers \code{c(lon, lat)} are WGS84; an sf point uses its own coordinate system.
\item \strong{An extent} returns exactly that area, snapped outward to whole cells:
an \code{sf::st_bbox()}, any sf geometry, or plain WGS84
\code{c(xmin, ymin, xmax, ymax)}.
\item \strong{A raster path} returns data on exactly that raster's grid and
coordinate system, which must be projected.
}
}

\section{Licence}{

Mapterhorn's data comes from many producers under their own open licences
(CC BY 4.0, Datenlizenz Deutschland, and others). The sources used are
listed in a message the first time each is fetched in a session, and by
\code{\link[=rvt_data_mapterhorn_sources]{rvt_data_mapterhorn_sources()}}. Credit Mapterhorn and each source; see
\url{https://mapterhorn.com/attribution} for the full terms.
}

\examples{
\donttest{
# the Matterhorn, 3 km across at Switzerland's 0.5 m, shown at 2 m
rvt_data_mapterhorn(c(7.6586, 45.9763), res = 2, size = 3000) |>
  rvt_hillshade() |>
  rvt_plot()

rvt_data_mapterhorn_sources(c(7.6586, 45.9763))
}
}
\seealso{
\code{\link[=rvt_data_mapterhorn_sources]{rvt_data_mapterhorn_sources()}}, and \code{\link[=rvt_data_lgln]{rvt_data_lgln()}} for Lower
Saxony's terrain, surface model and orthophotos at their native grid
}
