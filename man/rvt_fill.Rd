% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess.R
\name{rvt_fill}
\alias{rvt_fill}
\title{Fill gaps in a raster}
\usage{
rvt_fill(
  dem,
  out_path = fs::file_temp(ext = "tif"),
  max_distance = 100,
  smooth_iterations = 0,
  threads = rvt_threads(),
  overwrite = FALSE
)
}
\arguments{
\item{dem}{path to a DEM, or a vector of paths forming a mosaic (see
\code{\link[=rvt_mosaic]{rvt_mosaic()}}); mosaics are read across file boundaries, so tiles do not
produce seams. A terra \code{SpatRaster} works too: one read from a file is
used as it lies, while one terra computed in memory is written to a
temporary GeoTIFF first.}

\item{out_path}{output GeoTIFF path (default: a temp file). A folder that
does not exist yet is created.}

\item{max_distance}{how far to search for valid cells, in \strong{map units}
(metres, normally; default 100)}

\item{smooth_iterations}{3x3 smoothing passes applied to the filled cells
only (default 0)}

\item{threads}{C++ threads (see \code{\link[=rvt_threads]{rvt_threads()}})}

\item{overwrite}{recompute even if \code{out_path} exists}
}
\value{
\code{out_path}, invisibly
}
\description{
Interpolates across NoData holes so the surface is continuous. Worth doing
before anything else, because a hole is not simply "no answer here": it
spreads. The derivative metrics (\code{\link[=rvt_slope]{rvt_slope()}}, \code{\link[=rvt_curvature]{rvt_curvature()}}) lose a
ring of cells around every hole, and the horizon metrics treat a hole as
\emph{transparent} - light passes straight through ground that was never
measured - so a DSM with gaps in its canopy will read as brighter than the
real thing.
}
\section{How it works}{

GDAL's \code{GDALFillNodata()} (via \code{\link[gdalraster:fillNodata]{gdalraster::fillNodata()}}): for each cell
in a hole, the value is interpolated from the nearest valid cells in each
of four directions, weighted by inverse distance, followed by optional
smoothing passes over the interpolated values only.

Cells that already had data are never touched - verified bit for bit - so
filling can only add information, never alter what was measured.

Unlike the metrics here this is \strong{not tiled}: GDAL fills the band as a
whole. On a raster far larger than memory, fill the tiles individually
before mosaicking.
}

\section{Tuning}{

\itemize{
\item \code{max_distance} is how far the interpolation will reach to find valid
ground, in \strong{map units}. Holes wider than twice this are left partly
unfilled, which is usually what you want: a 2 km gap in coverage should
not be invented from its edges.
\item \code{smooth_iterations} runs 3x3 averaging passes over the filled cells,
softening the spokes that inverse-distance interpolation can leave in a
large hole. 0 is fine for scattered single-cell gaps; 2-3 helps on
anything larger.
}
}

\examples{
dem <- system.file("extdata", "dtm1.tif", package = "rvtr")
dem |> rvt_fill()
}
\seealso{
\code{\link[=rvt_smooth]{rvt_smooth()}}, which removes noise rather than gaps - run this
first, since smoothing cannot sensibly cross a hole.
}
