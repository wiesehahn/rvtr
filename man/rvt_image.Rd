% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/image.R
\name{rvt_image}
\alias{rvt_image}
\title{Save a raster or blend as an image}
\usage{
rvt_image(
  x,
  out_path = fs::file_temp(ext = "webp"),
  col = "Grays",
  range = NULL,
  pct = NULL,
  band = NULL,
  quality = 90,
  tile_size = NULL,
  threads = rvt_threads(),
  overwrite = FALSE,
  progress = FALSE
)
}
\arguments{
\item{x}{a raster path, or an \code{rvt_stack} from \code{\link[=rvt_blend]{rvt_blend()}}}

\item{out_path}{the image to write, ending in \code{.webp} or \code{.jpg}. Defaults to
a temporary \code{.webp} file. A folder that does not exist yet is created.}

\item{col}{palette name or colours for a single band, as in \code{\link[=rvt_plot]{rvt_plot()}}
(default \code{"Grays"}); see \code{\link[=rvt_palettes]{rvt_palettes()}}}

\item{range}{\code{c(lo, hi)} to stretch a single-band or RGB raster over, or
\code{NULL} (default) for its own minimum and maximum}

\item{pct}{\code{c(low, high)} percentages to cut from each tail instead, e.g.
\code{c(2, 98)}, measured with \code{\link[=rvt_range]{rvt_range()}}. Not with \code{range}.}

\item{band}{which band to draw, or three band numbers for red, green and
blue. Defaults to band 1, or 1-3 for a three-band raster.}

\item{quality}{compression quality, 1-100 (default 90)}

\item{tile_size}{tile edge in pixels; NULL picks a size targeting roughly
256 MB per working matrix}

\item{threads}{C++ threads (see \code{\link[=rvt_threads]{rvt_threads()}})}

\item{overwrite}{recompute even if \code{out_path} exists}

\item{progress}{report per-tile progress}
}
\value{
\code{out_path}, invisibly
}
\description{
Writes a WebP or JPEG picture of any raster or blend stack, at full
resolution: a colour palette and stretch for a single band, the colours as
they are for three bands. Where \code{\link[=rvt_plot]{rvt_plot()}} draws a quick look on screen,
this makes the file you put on a web page or in a report.
}
\details{
The format follows the file extension: \code{.webp} or \code{.jpg}. Images carry no
georeferencing; for a result to use in a GIS, keep the \code{.tif} the metric
function wrote.

\code{\link[=rvt_render]{rvt_render()}}, \code{\link[=rvt_relight]{rvt_relight()}} and \code{\link[=rvt_vat]{rvt_vat()}} write images directly too -
give them an image path - so their results need no second step.
}
\section{Choosing a format}{

Both are compressed for viewing, not for keeping values: the default quality
of 90 looks the same as the original, but the numbers are not preserved.
Keep the GeoTIFF for anything that needs them.

\strong{WebP} is the smaller file in every case measured - about 10\% smaller than
JPEG for photographs and shading, and a tenth of a compressed GeoTIFF - and
shows NoData as transparent.

\strong{JPEG} is the one to reach for when a tool cannot read WebP, or for images
wider than WebP's limit of 16,383 pixels (JPEG allows 65,500). It has no
transparency, so NoData is black.
}

\section{Stretch and colours}{

A single band is stretched across \code{range} and coloured with \code{col}, exactly
as in \code{\link[=rvt_plot]{rvt_plot()}}. \code{range = NULL} uses the raster's own minimum and
maximum, which a handful of extreme cells can easily spoil; \code{pct = c(2, 98)}
cuts percentiles off each tail instead, measured with \code{\link[=rvt_range]{rvt_range()}}. Both
are spelled and measured exactly as in \code{\link[=rvt_plot]{rvt_plot()}}, so what you settled on
while looking at the screen is what you write. Each metric's help page lists
recommended ranges. The range is fixed before any tile is read, so the
picture does not depend on \code{tile_size}.

Three bands - an RGB result such as \code{\link[=rvt_mstp]{rvt_mstp()}}, or a blend stack over an
orthophoto - are stretched over one range spanning all bands, and \code{col} is
not used. A blend stack is stretched layer by layer when it is blended (see
\code{\link[=rvt_blend]{rvt_blend()}}), so neither \code{range} nor \code{pct} applies to it.
}

\examples{
dem <- system.file("extdata", "dtm1.tif", package = "rvtr")
dem |> rvt_svf() |> rvt_image(range = c(0.7, 1))

# a percentile stretch, when the range isn't known in advance
dem |> rvt_svf() |> rvt_image(pct = c(2, 98))

# signed data: a diverging palette with a symmetric range
dem |> rvt_slrm() |> rvt_image(col = "Blue-Red 3", range = c(-1, 1))
}
\seealso{
\code{\link[=rvt_plot]{rvt_plot()}} for a quick look on screen, \code{\link[=rvt_render]{rvt_render()}} for blends
}
