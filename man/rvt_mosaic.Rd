% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mosaic.R
\name{rvt_mosaic}
\alias{rvt_mosaic}
\title{Present one or many rasters as a single dataset}
\usage{
rvt_mosaic(dem, vrt_path = tempfile(fileext = ".vrt"), cl_arg = NULL)
}
\arguments{
\item{dem}{a path, or a character vector of paths to mosaic. A directory,
or a vector of length 1 ending in \code{.vrt}, is returned unchanged.}

\item{vrt_path}{where to write the VRT (default: a temp file)}

\item{cl_arg}{extra arguments passed to \code{gdalbuildvrt}, e.g.
\code{c("-resolution", "highest")}}
}
\value{
path to a single dataset usable by \code{\link[=rvt_svf]{rvt_svf()}}, \code{\link[=rvt_openness]{rvt_openness()}},
\code{\link[=rvt_vat]{rvt_vat()}}. Pipe-friendly: \code{tiles |> rvt_mosaic() |> rvt_vat()}.
}
\description{
Passes a single path straight through. For several paths, builds a GDAL
virtual raster (VRT) over them and returns its path - a small XML file, no
pixel data is copied. All functions in this package accept either form, so
calling this directly is only necessary if you want to keep and reuse the
VRT.
}
\examples{
dem <- system.file("extdata", "dtm1.tif", package = "rvtr")
rvt_mosaic(dem)  # single path passes through unchanged
}
