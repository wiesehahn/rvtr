% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/horizon.R
\name{rvt_openness_negative}
\alias{rvt_openness_negative}
\title{Negative openness}
\usage{
rvt_openness_negative(
  dem,
  out_path = fs::file_temp(ext = "tif"),
  num_directions = 16,
  reach = 10,
  noise_removal = 0,
  pyramid_px = 100,
  pyramid_factor = 4,
  tile_size = NULL,
  threads = rvt_threads(),
  overwrite = FALSE,
  progress = FALSE
)
}
\arguments{
\item{dem}{path to a DEM, or a vector of paths forming a mosaic (see
\code{\link[=rvt_mosaic]{rvt_mosaic()}}); mosaics are read across file boundaries, so tiles do not
produce seams}

\item{out_path}{output GeoTIFF path (default: a temp file)}

\item{num_directions}{number of compass directions scanned (default 16)}

\item{reach}{look this far in \strong{map units} (metres, normally) using a
multi-resolution search, instead of \code{radius_max} pixels at full
resolution. Cost then grows with the logarithm of the distance rather
than in proportion to it, which is what makes a reach of hundreds of
metres affordable on fine data. \code{radius_max} and \code{noise_removal} are
ignored when this is set. See \link{rvt_reach}.}

\item{noise_removal}{0-3; raises the minimum search radius to ignore
near-cell noise (0, 10, 20 or 40 percent of \code{reach})}

\item{pyramid_px, pyramid_factor}{shape of that multi-resolution search:
pixels scanned per level (default 100, the accuracy knob) and how much
coarser each level is than the last (default 4). Only used with \code{reach}.}

\item{tile_size}{tile edge in pixels; NULL picks a size targeting roughly
256 MB per working matrix}

\item{threads}{C++ threads (see \code{\link[=rvt_threads]{rvt_threads()}})}

\item{overwrite}{recompute even if \code{out_path} exists}

\item{progress}{report per-tile progress}
}
\value{
\code{out_path}, invisibly
}
\description{
The same measurement as \code{\link[=rvt_openness]{rvt_openness()}}, made on the terrain turned upside
down. Concave features become the prominent ones: ditches, pits, hollow
ways and quarry scoops read high, where positive openness favours banks,
mounds and ridges.
}
\details{
Note that only the \emph{shape} preference is inverted, not the numbers. Flat
ground still gives exactly 90 degrees, and a cell in a pit reads \emph{above}
90 here - because on the inverted surface that pit is a mound. Values are
in degrees and, as with positive openness, are not restricted to 0-90.
}
\section{How it works}{

Elevations are negated, and then exactly the horizon search described in
\code{\link[=rvt_openness]{rvt_openness()}} runs on the result - same directions, same sampling, same
\verb{90 - mean(horizon angle)}. Nothing else differs, so the two are directly
comparable and can be differenced.
}

\section{Tuning}{

Identical to \code{\link[=rvt_openness]{rvt_openness()}}. Use the same \code{reach} for both if you
intend to compare or difference them - a mismatch there makes the two
images respond to different feature sizes and the comparison stops meaning
much.
}

\section{Recommended settings}{

Worth being clear about, since the name invites the assumption: \strong{negative
openness is not the inverse of positive openness.} It carries genuinely
different information rather than the same image with the sign flipped.
Where positive openness picks out the convexities - the ridge \emph{between} two
hollow ways, the raised rim of a crater - negative openness picks out the
deepest parts of the concavities: the hollow way itself, the floor of a
gorge, the foot of a cliff. Computing both and reading them together is the
point.

Kokalj and Hesse (2017) recommend displaying it with an \strong{inverted}
greyscale, dark for high values, so that concave features read dark in both
images and the pair stay visually consistent. Their suggested stretch is
60-95 degrees on varied terrain, 75-95 on very flat ground and 45-95 on
steep or complex ground. Radius and directions as for \code{\link[=rvt_svf]{rvt_svf()}}: 10 m,
16 directions.
}

\examples{
dem <- system.file("extdata", "dtm1.tif", package = "rvtr")
rvt_openness_negative(dem)
}
\references{
Kokalj, Ž. and Hesse, R. (2017) \emph{Airborne Laser Scanning Raster Data
Visualization: A Guide to Good Practice}. Ljubljana: Založba ZRC.
\doi{10.3986/9789612549848}
}
\seealso{
\code{\link[=rvt_openness]{rvt_openness()}} for the convex counterpart.
}
