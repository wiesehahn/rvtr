% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/palettes.R
\name{rvt_palettes}
\alias{rvt_palettes}
\title{Browse the colour palettes}
\usage{
rvt_palettes(type = NULL, n = 64)
}
\arguments{
\item{type}{\code{NULL} (default) for all of them, or one of \code{"sequential"},
\code{"diverging"}, \code{"divergingx"}, \code{"qualitative"}}

\item{n}{number of colours drawn in each ramp (default 64)}
}
\value{
the palette names shown, invisibly
}
\description{
Draws every palette \code{\link[=rvt_plot]{rvt_plot()}} accepts by name as a colour ramp, grouped
by kind, so you can pick one by eye rather than by guessing a name. Pass the
name you like straight to \code{rvt_plot()}: \code{rvt_plot(svf, "Viridis")}.
}
\details{
Which kind to reach for depends on what the values mean:
\itemize{
\item \strong{sequential} for magnitudes running one way - sky-view factor, slope,
openness, elevation. Most relief visualizations want one of these, and a
plain \code{"Grays"} is hard to beat for reading shape.
\item \strong{diverging} for signed results centred on zero - curvature, \code{\link[=rvt_log]{rvt_log()}},
\code{\link[=rvt_slrm]{rvt_slrm()}}, \code{\link[=rvt_msrm]{rvt_msrm()}}, \code{\link[=rvt_dev]{rvt_dev()}} - so convex and concave read as
opposites. Keep the stretch symmetric about zero, as \code{\link[=rvt_plot]{rvt_plot()}}
explains, or zero will not land on the neutral middle.
\item \strong{divergingx} join two sequential ramps with no requirement that they
balance. Many are familiar from other tools (\code{"RdBu"}, \code{"PuOr"},
\code{"Spectral"}), but one arm can end much darker than the other, so equal
positive and negative values need not look equally strong: \code{"PuOr"}
ends at lightness 10 on its purple side against 31 on its orange side.
\code{"Cividis"} is filed here too, although it runs dark to light and is
really sequential. Prefer \strong{diverging} when that balance matters.
\item \strong{qualitative} for classes with no order, such as \code{\link[=rvt_geomorphons]{rvt_geomorphons()}}:
a ramp would imply an order the classes do not have.
}
}
\examples{
rvt_palettes("diverging")
names <- rvt_palettes("sequential")
}
\seealso{
\code{\link[=rvt_plot]{rvt_plot()}}
}
