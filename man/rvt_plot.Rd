% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{rvt_plot}
\alias{rvt_plot}
\title{Quick-look plot of a raster}
\usage{
rvt_plot(
  path,
  max_dim = 1000,
  col = grDevices::hcl.colors(256, "Grays"),
  legend = FALSE,
  axes = FALSE,
  band = NULL,
  ...
)
}
\arguments{
\item{path}{path to a single-band raster}

\item{max_dim}{longest side, in pixels, to render at (default 1000);
downsampling happens on read via GDAL, not by loading the full raster
first}

\item{col}{colour ramp (default: a grey ramp), passed to
\code{\link[gdalraster:plot_raster]{gdalraster::plot_raster()}}'s \code{col_map_fn}}

\item{legend}{show a colour scale (default \code{FALSE}, just the image)}

\item{axes}{show axes with coordinates (default \code{FALSE})}

\item{band}{which band to draw, for multi-band results such as
\code{\link[=rvt_multi_hillshade]{rvt_multi_hillshade()}}; or three band numbers to draw as red, green and
blue. Defaults to band 1, except for a 3-band raster, which is drawn as
RGB (which is what \code{\link[=rvt_mstp]{rvt_mstp()}} wants).}

\item{...}{passed on to \code{\link[gdalraster:plot_raster]{gdalraster::plot_raster()}}, e.g. \code{main = "..."}
for a title}
}
\value{
\code{path}, invisibly. \code{path} is the first argument, so this is
pipe-friendly: \code{path |> rvt_plot()}.
}
\description{
Renders a single-band raster - typically output from \code{\link[=rvt_svf]{rvt_svf()}},
\code{\link[=rvt_openness]{rvt_openness()}} or \code{\link[=rvt_vat]{rvt_vat()}} - as an image. A thin wrapper around
\code{\link[gdalraster:plot_raster]{gdalraster::plot_raster()}}, which does the actual work: correct
orientation, a colour scale stretched to the data's own range, and NoData
handling. This just adds a downsampled read (so a huge raster still
renders fast) and a path-in, path-out interface for piping.
}
\examples{
dem <- system.file("extdata", "dtm1.tif", package = "rvtr")
rvt_plot(dem)
rvt_plot(dem, legend = TRUE, axes = TRUE, main = basename(dem))
}
