% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/blend.R
\name{rvt_range}
\alias{rvt_range}
\title{Stretch range for a layer}
\usage{
rvt_range(path, pct = NULL, band = 1L, max_dim = 1000L)
}
\arguments{
\item{path}{raster to measure}

\item{pct}{optional \code{c(low, high)} percentages to cut from each tail, e.g.
\code{c(2, 98)}. \code{NULL} (default) uses the full minimum and maximum.}

\item{band}{band to measure (default 1)}

\item{max_dim}{when \code{pct} is given, the longest side of the decimated read
the percentiles are taken from (default 1000)}
}
\value{
\code{c(lo, hi)}
}
\description{
The \code{c(lo, hi)} a layer is stretched over before blending, read from the
raster itself. Evaluate it when you build the stack, so the numbers are
fixed before any tile is touched.
}
\details{
That is the whole point: a range derived per tile would give the same ground
different values depending on which tile it landed in, which is the one
thing this package will not do. Passing a frozen pair sidesteps the question
entirely.
}
\examples{
dem <- system.file("extdata", "dtm1.tif", package = "rvtr")
svf <- rvt_svf(dem)
rvt_range(svf)
rvt_range(svf, pct = c(2, 98))
}
\seealso{
\code{\link[=rvt_blend]{rvt_blend()}}
}
