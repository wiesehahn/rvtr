% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/relight.R
\name{rvt_relight}
\alias{rvt_relight}
\title{Relight an orthophoto with its terrain}
\usage{
rvt_relight(
  ortho,
  dem,
  out_path = fs::file_temp(ext = "tif"),
  style = "default",
  sun_azimuth = NULL,
  sun_elevation = NULL,
  softness = NULL,
  reach = NULL,
  saturation = NULL,
  contrast = NULL,
  vegetation = NULL,
  sun_strength = NULL,
  sky_strength = NULL,
  bounce = NULL,
  occlusion = NULL,
  sun_color = NULL,
  sky_color = NULL,
  quality = 90,
  tile_size = NULL,
  threads = rvt_threads(),
  overwrite = FALSE,
  progress = FALSE
)
}
\arguments{
\item{ortho}{path to a three-band orthophoto}

\item{dem}{surface or terrain model covering the orthophoto's extent, or a
vector of paths to mosaic. Its resolution may differ from the photo's by a
whole factor: a 1 m surface under a 0.2 m photo is lit at 1 m and the
light upsampled, so the result keeps the photo's full resolution.}

\item{out_path}{where to write; defaults to a temporary file}

\item{style}{name of a style in \link{rvt_relight_styles} (default \code{"default"})}

\item{sun_azimuth, sun_elevation}{sun position in degrees}

\item{softness}{spread of the sun positions averaged for soft shadows, in
degrees; 0 for hard shadows}

\item{reach}{how far shadows are traced, in map units}

\item{saturation}{colour strength of the photo; 1 leaves it unchanged}

\item{contrast}{contrast of the result; 1 leaves it unchanged}

\item{vegetation}{extra colour given to green ground only, 0 for none.
Deepens foliage, pulls warm greens apart from cool ones and separates
light from dark canopy, leaving roofs, roads, water and bare soil alone}

\item{sun_strength, sky_strength}{strength of the sun and sky light}

\item{bounce}{share of sun light that still reaches ground in cast shadow}

\item{occlusion}{how much sky light enclosed ground loses, 0 to 1}

\item{sun_color, sky_color}{colours of the sun and sky light}

\item{quality}{WebP quality, 1 to 100 (default 90)}

\item{tile_size}{tile edge in pixels; NULL picks a size targeting roughly
256 MB per working matrix}

\item{threads}{C++ threads (see \code{\link[=rvt_threads]{rvt_threads()}})}

\item{overwrite}{recompute even if \code{out_path} exists}

\item{progress}{report per-tile progress}
}
\value{
\code{out_path}, invisibly
}
\description{
Lights an aerial photo with the terrain beneath it, the way a rendered
landscape model is lit: a warm sun on slopes that face it, soft cast
shadows, cool sky light that colours the shadows, and less of that sky
light in enclosed ground. The result reads as a small physical model rather
than a flat photo with shading laid over it.
}
\details{
Use a \strong{surface model} for \code{dem} when the photo shows trees and buildings,
so that they cast shadows too, and a terrain model when you want only the
shape of the ground.
}
\section{How it works}{

Hillshade, sky-view factor and cast shadow are computed from \code{dem}. Shadows
are averaged over sun positions \code{softness} degrees apart, which softens
their edges. Light is then built per cell as sun light plus sky light:
\itemize{
\item \strong{sun light}: \code{sun_color} times \code{sun_strength}, scaled by how squarely
the ground faces the sun, and reduced in cast shadow to a fraction
\code{bounce} of full strength rather than to nothing;
\item \strong{sky light}: \code{sky_color} times \code{sky_strength}, reduced by up to
\code{occlusion} where the sky-view factor says the ground is enclosed.
}

The photo's colours are strengthened by \code{saturation}, multiplied by that
light, and given \code{contrast}. Because the light is coloured, shadows pick up
a cool cast from the sky light; the tint is subtle in deep shadow, where
little light of any colour remains.

Sunlit open ground keeps close to the photo's brightness, but anything in
shadow is darker, so heavily wooded or built-up areas come out noticeably
darker overall. Raise \code{bounce} or lower \code{occlusion} to lighten them.
}

\section{Styles}{

\code{style} picks a named set of settings from \link{rvt_relight_styles}, such as
\code{"golden_hour"} or \code{"winter"}. Any setting given here overrides the style,
so \verb{style = "winter", sun_elevation = 10} is a winter light with an even
lower sun.
}

\section{Output}{

A three-band, 8-bit Cloud-Optimized GeoTIFF compressed with WebP. \code{quality}
trades file size for fidelity; the default of 90 is visually lossless for
imagery at typical viewing sizes.

Give \code{out_path} a \code{.webp} or \code{.jpg} extension to write a plain picture
instead, for web pages and reports: no georeferencing, and no GeoTIFF made
along the way. WebP is the smaller of the two; JPEG is readable everywhere
and allows larger images. See \code{\link[=rvt_image]{rvt_image()}} for the comparison.
}

\examples{
\donttest{
pt <- c(9.9464, 51.6317)                      # Burg Hardenberg
rgb <- rvt_data_lgln(pt, "rgb", res = 1)
dsm <- rvt_data_lgln(pt, "dsm")
rgb |> rvt_relight(dsm) |> rvt_plot(minmax_def = c(0, 0, 0, 255, 255, 255))
rgb |> rvt_relight(dsm, style = "golden_hour") |>
  rvt_plot(minmax_def = c(0, 0, 0, 255, 255, 255))
}
}
\seealso{
\link{rvt_relight_styles}, \code{\link[=rvt_blend]{rvt_blend()}} for general layer blending,
\code{\link[=rvt_data_lgln]{rvt_data_lgln()}} for sample data
}
