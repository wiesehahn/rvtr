% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess.R
\name{rvt_smooth}
\alias{rvt_smooth}
\title{Feature-preserving smoothing}
\usage{
rvt_smooth(
  dem,
  out_path = fs::file_temp(ext = "tif"),
  radius = 5,
  norm_diff = 15,
  iterations = 3,
  max_diff = 0.5,
  tile_size = NULL,
  threads = rvt_threads(),
  overwrite = FALSE,
  progress = FALSE
)
}
\arguments{
\item{dem}{path to a DEM, or a vector of paths forming a mosaic (see
\code{\link[=rvt_mosaic]{rvt_mosaic()}}); mosaics are read across file boundaries, so tiles do not
produce seams. A terra \code{SpatRaster} works too: one read from a file is
used as it lies, while one terra computed in memory is written to a
temporary GeoTIFF first.}

\item{out_path}{output GeoTIFF path (default: a temp file)}

\item{radius}{how far the normal filter reaches, in \strong{map units} (metres,
normally; default 5)}

\item{norm_diff}{angle in degrees beyond which neighbouring ground is
treated as a different surface and not averaged across (default 15)}

\item{iterations}{elevation-update passes (default 3)}

\item{max_diff}{largest change any cell may undergo, in elevation units
(default 0.5)}

\item{tile_size}{tile edge in pixels; NULL picks a size targeting roughly
256 MB per working matrix}

\item{threads}{C++ threads (see \code{\link[=rvt_threads]{rvt_threads()}})}

\item{overwrite}{recompute even if \code{out_path} exists}

\item{progress}{report per-tile progress}
}
\value{
\code{out_path}, invisibly
}
\description{
Removes noise from a surface while leaving breaks of slope sharp. An
ordinary averaging or Gaussian filter cannot tell a noisy cell from a
terrace edge and rounds off both, which is self-defeating here: the edges
are what most of these metrics exist to find.
}
\details{
Worth reaching for when a DEM is speckled - gridded finer than the point
density really supports, or affected by flight-strip misalignment - and
before \code{\link[=rvt_curvature]{rvt_curvature()}}, \code{\link[=rvt_log]{rvt_log()}} or \code{\link[=rvt_geomorphons]{rvt_geomorphons()}}, all of which
amplify high-frequency noise by construction.
}
\section{How it works}{

The algorithm is Sun, Rosin, Martin and Langbein's (2007), as used by
WhiteboxTools' \code{FeaturePreservingSmoothing}. It smooths the surface
\emph{normals} rather than the elevations:
\itemize{
\item A unit normal is computed per cell, from the same central differences
\code{\link[=rvt_slope]{rvt_slope()}} uses.
\item Each normal is replaced by a weighted average of those within \code{radius},
with the weight falling to zero once a neighbour's normal differs by more
than \code{norm_diff} degrees. Across a break of slope the two sides do not
see each other at all, so the edge survives, while noise - small random
deviations well inside the threshold - averages away.
\item Elevations are then nudged repeatedly towards agreement with those
smoothed normals: each neighbour's plane implies a height for this cell,
and the cell moves to their mean.
}

That last step works on immediate neighbours only, not the whole \code{radius}
window - the radius belongs to the normal filter. Letting it reach further
lets cells on opposite sides of a scarp pull on each other and smears the
very edge the normal filter preserved.

Total movement is capped at \code{max_diff}, so no cell can drift far from
measured ground however many iterations are run. Tiles overlap by the full
reach of the three stages, so the result does not depend on \code{tile_size}.
}

\section{Tuning}{

\itemize{
\item \code{norm_diff} is the one that matters. It is the angle, in degrees, beyond
which two pieces of ground are treated as belonging to different
surfaces. Low values (5-10) preserve almost every edge and remove only
the finest noise; high values (25-40) smooth much harder and start
rounding real breaks. The default 15 follows WhiteboxTools.
\item \code{radius} sets how far the averaging reaches, in \strong{map units}. Larger
removes more noise and costs proportionally more - the window is
circular, so cost grows with the square.
\item \code{iterations} repeats only the elevation step, against normals that are
filtered once. Three is usually enough; more mostly runs into \code{max_diff}.
\item \code{max_diff} is a safety rail in elevation units. Lower it if you want to
guarantee the surface stays close to what was measured.
}
}

\examples{
dem <- system.file("extdata", "dtm1.tif", package = "rvtr")

# the usual preparation, as a pipeline
dem |> rvt_fill() |> rvt_smooth() |> rvt_curvature() |> rvt_plot()
}
\references{
Sun, X., Rosin, P.L., Martin, R.R. and Langbein, F.C. (2007) Fast and
effective feature-preserving mesh denoising. \emph{IEEE Transactions on
Visualization and Computer Graphics} 13(5), 925-938.
\doi{10.1109/TVCG.2007.1065}
}
\seealso{
\code{\link[=rvt_fill]{rvt_fill()}}, which closes gaps rather than removing noise. Run it
first: smoothing cannot sensibly average across a hole.
}
