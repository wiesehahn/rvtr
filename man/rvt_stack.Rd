% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/blend.R
\name{rvt_stack}
\alias{rvt_stack}
\title{Start a blend stack}
\usage{
rvt_stack(base, range = NULL, invert = FALSE)
}
\arguments{
\item{base}{path to the background raster}

\item{range}{\code{c(lo, hi)} to stretch over, or \code{NULL} (default) for the
raster's own minimum and maximum. See \code{\link[=rvt_range]{rvt_range()}}.}

\item{invert}{flip the layer after stretching, so high reads dark}
}
\value{
an \code{rvt_stack}
}
\description{
The background layer a stack is built on. Only needed when the background
itself needs a \code{range} or \code{invert}; otherwise pipe the raster straight into
\code{\link[=rvt_blend]{rvt_blend()}}, which starts a stack for you.
}
\examples{
dem <- system.file("extdata", "dtm1.tif", package = "rvtr")
dem |> rvt_hillshade() |> rvt_stack(range = c(0, 1))
}
\seealso{
\code{\link[=rvt_blend]{rvt_blend()}} to add layers, \code{\link[=rvt_render]{rvt_render()}} to write the result
}
