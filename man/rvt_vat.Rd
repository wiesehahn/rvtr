% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vat.R
\name{rvt_vat}
\alias{rvt_vat}
\title{Archaeological VAT (combined)}
\usage{
rvt_vat(
  dem,
  out_path = tempfile(fileext = ".tif"),
  num_directions = 16,
  presets = list(rvt_preset_general, rvt_preset_flat),
  tile_size = NULL,
  threads = rvt_threads(),
  rvt_compat = FALSE,
  overwrite = FALSE,
  progress = FALSE
)
}
\arguments{
\item{dem}{path to a DEM, or a vector of paths forming a mosaic (see
\code{\link[=rvt_mosaic]{rvt_mosaic()}}); mosaics are read across file boundaries, so tiles do not
produce seams}

\item{out_path}{output GeoTIFF path (default: a temp file)}

\item{num_directions}{number of search directions (RVT default 16)}

\item{presets}{list of two parameter sets, general first then flat; see
\link{rvt_presets}}

\item{tile_size}{tile edge in pixels; NULL picks a size targeting roughly
256 MB per working matrix}

\item{threads}{C++ threads (see \code{\link[=rvt_threads]{rvt_threads()}})}

\item{rvt_compat}{reproduce a bug in rvt-py's blend_overlay() that makes
the openness layer's declared 50\% opacity have no effect. \code{FALSE}
(default) applies the opacity as configured; \code{TRUE} is only useful for
comparing output against rvt-py.}

\item{overwrite}{recompute even if \code{out_path} exists}

\item{progress}{report per-tile progress}
}
\value{
\code{out_path}, invisibly. \code{dem} is the first argument, so this is
pipe-friendly: \code{dem |> rvt_vat("vat.tif")}.
}
\description{
Visualization for Archaeological Topography: a blend of hillshade, slope,
positive openness and sky-view factor, rendered once with a general-terrain
preset and once with a flat-terrain preset and averaged. Values are 0-1.
}
\examples{
dem <- system.file("extdata", "dtm1.tif", package = "rvtr")
rvt_vat(dem)
}
